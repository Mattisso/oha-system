import { NttcomptebalanceFilterPipe } from './nttcomptebalance-filter.pipe';

describe('NttcomptebalanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NttcomptebalanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
