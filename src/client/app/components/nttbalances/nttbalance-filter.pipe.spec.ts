import { NttbalancePipe } from './nttbalance-filter.pipe';

describe('NttbalancePipe', () => {
  it('create an instance', () => {
    const pipe = new NttbalancePipe();
    expect(pipe).toBeTruthy();
  });
});
