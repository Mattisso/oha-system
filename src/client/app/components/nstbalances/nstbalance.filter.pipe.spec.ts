import { NstbalancePipe } from './nstbalance.filter.pipe';

describe('NstbalancePipe', () => {
  it('create an instance', () => {
    const pipe = new NstbalancePipe();
    expect(pipe).toBeTruthy();
  });
});
